import { Data, Override } from "framer"

const data = Data({})

export function MoveLeft(): Override {
    return {
        animate: {
            left: -180,
        },
        transition: {
            duration: 4,
            easings: "linear",
        },
    }
}
